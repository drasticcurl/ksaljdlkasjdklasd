/**
 * Configuración global de la suite de tests del frontend.
 *
 * Registra los matchers de `@testing-library/jest-dom` (p. ej. `toBeDisabled`,
 * `toHaveTextContent`) en el `expect` de vitest para los tests de componentes.
 */
import '@testing-library/jest-dom/vitest';
